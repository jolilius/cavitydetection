---
phase: 04-per-step-data-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - openevolve/test_consolidation.py
  - openevolve/consolidate_results.py
  - openevolve/results_loader.py
autonomous: true
requirements:
  - CKPT-01
  - CKPT-02
  - CKPT-03

must_haves:
  truths:
    - "consolidate_experiment() produces one row per checkpoint_N directory, sorted numerically (5 before 10 before 15)"
    - "Each row contains the six Phase 4 schema fields: checkpoint_iteration, best_found_at_iteration, code, combined_score, mem_score, time_score"
    - "Row includes backward-compat alias iteration = checkpoint_n so existing sort logic in load_results() keeps working"
    - "time_score is JSON null in every row (D-01)"
    - "combined_score equals mem_score in every row (D-02)"
    - "Top-level container key in results.json is still iterations (not checkpoints) (D-03)"
    - "D-04: _extract_iterations() is replaced in-place in consolidate_results.py; consolidate_experiment() public signature is unchanged"
    - "D-05: checkpoint_iteration equals integer folder N from checkpoint_N/; best_found_at_iteration equals JSON 'iteration' field (not current_iteration)"
    - "load_results() exposes the new columns checkpoint_iteration, best_found_at_iteration, code, combined_score, time_score in addition to existing columns"
  artifacts:
    - path: "openevolve/test_consolidation.py"
      provides: "Wave 0 failing test stubs for checkpoint consolidation, threading, and loader columns"
      contains: "def test_checkpoint_based_consolidation"
    - path: "openevolve/consolidate_results.py"
      provides: "Checkpoint-scanning _extract_iterations() replacing best/-only fallback"
      contains: "checkpoints_dir = os.path.join(output_dir"
    - path: "openevolve/results_loader.py"
      provides: "Extended column ordering covering Phase 4 fields"
      contains: "checkpoint_iteration"
  key_links:
    - from: "openevolve/consolidate_results.py::_extract_iterations"
      to: "openevolve/results_loader.py::load_results"
      via: "shared row schema (iteration alias + new columns)"
      pattern: "checkpoint_iteration"
    - from: "openevolve/test_consolidation.py::test_checkpoint_based_consolidation"
      to: "openevolve/consolidate_results.py::consolidate_experiment"
      via: "synthetic checkpoints/checkpoint_N/ tree + assertion on iterations rows"
      pattern: "checkpoints/checkpoint_"
---

<objective>
Rewrite the consolidation core so `results.json` is built from `checkpoints/checkpoint_N/` directories instead of `best/best_program_info.json`, and extend `load_results()` to surface the new Phase 4 columns. Wave 0 test stubs go in first (per VALIDATION.md), then the implementation makes them pass.

Purpose: Satisfy CKPT-01 (checkpoint-sorted scan), CKPT-02 (six schema fields per row), and CKPT-03 (DataFrame surfaces those fields) — the data layer that downstream plans (explanations, legacy regeneration) consume.

Output: Updated `consolidate_results.py`, updated `results_loader.py`, three new tests in `test_consolidation.py`, and `test_consolidation.py` passing end-to-end (including the 6 existing tests).
</objective>

<execution_context>
@/home/jolilius/.claude/get-shit-done/workflows/execute-plan.md
@/home/jolilius/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-per-step-data-pipeline/04-CONTEXT.md
@.planning/phases/04-per-step-data-pipeline/04-RESEARCH.md
@.planning/phases/04-per-step-data-pipeline/04-PATTERNS.md
@.planning/phases/04-per-step-data-pipeline/04-VALIDATION.md
@openevolve/consolidate_results.py
@openevolve/results_loader.py
@openevolve/test_consolidation.py

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From `openevolve/consolidate_results.py` (UNCHANGED public API):
- `consolidate_experiment(output_dir, program="cavitydetection", prompt_variant=None, baseline_accesses=128_862_705, explanations=None, explanation_prompt_text=None, prompt_version="1.0") -> dict`
- `_extract_iterations(output_dir, best_info, baseline_accesses, explanations=None) -> list` — signature stays the same; body is replaced.
- Helper `_load_best_result(output_dir) -> dict` already exists and is still called by `consolidate_experiment` line ~74 — leave it; new `_extract_iterations` ignores `best_info` for row building (kept in signature only for backward compatibility).

From `openevolve/results_loader.py`:
- `load_results(filepath: str) -> pd.DataFrame` — column ordering block at lines 142-156 (empty-DataFrame columns) and 171-189 (primary/secondary/explanation/metadata).
- `load_all_results(results_root="openevolve_output") -> pd.DataFrame` — no body change required; it concatenates outputs of `load_results`.

Real checkpoint JSON shape (verified from `openevolve_output/baseline/checkpoints/checkpoint_10/best_program_info.json`):
```
{ "id": "...", "iteration": 5, "current_iteration": 10,
  "metrics": { "mem_score": 1.1549... },
  "timestamp": ..., "saved_at": ... }
```
Note: `iteration` (best-found generation) diverges from `current_iteration` (checkpoint-write generation) in every dir except `checkpoint_5`. Use folder N for `checkpoint_iteration`; use JSON `iteration` for `best_found_at_iteration` (D-05).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Wave 0 failing test stubs in test_consolidation.py</name>
  <files>openevolve/test_consolidation.py</files>
  <read_first>
    - openevolve/test_consolidation.py (full file — see existing fixture style at lines 34-54 and 150-183)
    - .planning/phases/04-per-step-data-pipeline/04-VALIDATION.md (Wave 0 Requirements section)
    - .planning/phases/04-per-step-data-pipeline/04-PATTERNS.md (test fixture pattern under "openevolve/test_consolidation.py — add checkpoint-based tests")
  </read_first>
  <action>
    Add three new test functions to `openevolve/test_consolidation.py` BEFORE making any changes to `consolidate_results.py` or `results_loader.py`. The tests must fail against the current implementation (they exercise the new schema), and pass after Tasks 2 and 3 land.

    Function 1: `test_checkpoint_based_consolidation()`. Inside a `tempfile.TemporaryDirectory()`, create a synthetic `checkpoints/checkpoint_N/` tree for N in {5, 10, 15}. In each `checkpoint_N/` dir write:
    - `best_program_info.json` with keys `iteration` (best-found, e.g. 5, 5, 10 for N=5,10,15 respectively), `current_iteration` (= N), `metrics` containing `mem_score` (e.g. 1.10, 1.15, 1.20), `timestamp`.
    - `best_program.c` with a short synthetic string like `f"/* checkpoint {N} */"` (per Pitfall 3, keep it short to stay fast).
    Call `consolidate_experiment(tmpdir, prompt_variant="test_ckpt")`. Assert: `len(result["iterations"]) == 3`; each row has keys `checkpoint_iteration`, `best_found_at_iteration`, `code`, `combined_score`, `mem_score`, `time_score`, `iteration`; `row["time_score"] is None`; `row["combined_score"] == row["mem_score"]`; `row["iteration"] == row["checkpoint_iteration"]`; the list `[r["checkpoint_iteration"] for r in result["iterations"]]` equals `[5, 10, 15]` (numeric sort, NOT lexicographic); for N=10 specifically, `row["best_found_at_iteration"] == 5` and `row["checkpoint_iteration"] == 10` (the divergence per D-05); `row["code"]` contains the synthetic checkpoint marker substring. End with `print("✓ test_checkpoint_based_consolidation passed")`.

    Function 2: `test_checkpoint_load_results()`. Build a synthetic checkpoint tree as in Function 1 (or reuse helper), call `consolidate_experiment(tmpdir, prompt_variant="test_ckpt")`, then call `load_results(os.path.join(tmpdir, "results.json"))`. Assert: `len(df) == 3`; the columns set contains `checkpoint_iteration`, `best_found_at_iteration`, `code`, `combined_score`, `time_score`, `mem_score`, `iteration`, `prompt`; `df.iloc[0]["checkpoint_iteration"] == 5`; `df.iloc[2]["checkpoint_iteration"] == 15`; `df["time_score"].isna().all()` (because JSON null becomes NaN through pandas); `(df["combined_score"] == df["mem_score"]).all()`. End with `print("✓ test_checkpoint_load_results passed")`.

    Function 3: `test_checkpoint_explanation_threading()`. Build the same synthetic checkpoint tree. Pass `explanations={5: "first delta", 10: "second delta", 15: "third delta"}` (dict keyed by folder N, per Pitfall 2) to `consolidate_experiment(tmpdir, prompt_variant="test_ckpt", explanations=explanations)`. Assert each row in `result["iterations"]` has `explanation` matching the dict value at its `checkpoint_iteration`. Then call `load_results` on the same `results.json` and assert `df["explanation"].notna().all()` and `df.iloc[0]["explanation"] == "first delta"`. End with `print("✓ test_checkpoint_explanation_threading passed")`.

    Add the three test calls to the `if __name__ == "__main__":` runner block (around lines 379-397) after the existing explanation tests, inside a new printed banner `"Running Phase 4 checkpoint tests"`. Imports needed: `from results_loader import load_results` and `import pandas as pd` (the existing tests already import these inline). Use the existing assertion / print style.

    Do NOT modify `consolidate_results.py` or `results_loader.py` in this task — these tests SHOULD fail right now (RED step of TDD).
  </action>
  <verify>
    <automated>cd /home/jolilius/home/src/cavitydetection &amp;&amp; ../openevolve/.venv/bin/python -c "import ast; tree = ast.parse(open('openevolve/test_consolidation.py').read()); names = [n.name for n in tree.body if isinstance(n, ast.FunctionDef)]; assert 'test_checkpoint_based_consolidation' in names, 'missing test_checkpoint_based_consolidation'; assert 'test_checkpoint_load_results' in names, 'missing test_checkpoint_load_results'; assert 'test_checkpoint_explanation_threading' in names, 'missing test_checkpoint_explanation_threading'; print('OK: 3 new test fns defined')"</automated>
    <automated>cd /home/jolilius/home/src/cavitydetection &amp;&amp; ! ../openevolve/.venv/bin/python openevolve/test_consolidation.py 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `openevolve/test_consolidation.py` defines top-level functions `test_checkpoint_based_consolidation`, `test_checkpoint_load_results`, `test_checkpoint_explanation_threading` (AST check above passes).
    - Each new test function uses `tempfile.TemporaryDirectory()` and creates at least 3 `checkpoint_N/` subdirectories with both `best_program_info.json` and `best_program.c`.
    - Test 1 asserts on row keys `checkpoint_iteration`, `best_found_at_iteration`, `code`, `combined_score`, `mem_score`, `time_score`, `iteration` and on numeric sort order `[5, 10, 15]`.
    - Test 2 asserts `df["time_score"].isna().all()` and `(df["combined_score"] == df["mem_score"]).all()`.
    - Test 3 uses an explanations dict keyed by integer folder N (not by JSON `iteration` field).
    - All six existing tests in the file still run (the runner block at the bottom still calls `test_consolidation()`, `test_consolidate_with_explanations()`, etc.).
    - The runner block exits with non-zero status when invoked now (RED state: existing implementation cannot satisfy the new tests). The negated-shell check above confirms `! python ... | tail -5` returns success because the test runner exits non-zero on failed asserts.
    - Each new test ends with a `print("✓ ... passed")` line matching the existing style.
  </acceptance_criteria>
  <done>Three new test functions exist in `openevolve/test_consolidation.py`, are wired into the `__main__` runner block, and the runner exits non-zero with the existing code (proving the new tests are exercising new behavior, not duplicating old).</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite _extract_iterations() in consolidate_results.py to scan checkpoints</name>
  <files>openevolve/consolidate_results.py</files>
  <read_first>
    - openevolve/consolidate_results.py (full file — current `_extract_iterations` lines 168-236, caller in `consolidate_experiment` lines 87-98)
    - .planning/phases/04-per-step-data-pipeline/04-CONTEXT.md (decisions D-01 through D-05)
    - .planning/phases/04-per-step-data-pipeline/04-RESEARCH.md (Pattern 1 + Pitfalls 1, 2, 4)
    - .planning/phases/04-per-step-data-pipeline/04-PATTERNS.md (`openevolve/consolidate_results.py — replace _extract_iterations()` section)
    - openevolve/openevolve_output/baseline/checkpoints/checkpoint_5/best_program_info.json (one real example to ground assumptions)
    - openevolve/openevolve_output/baseline/checkpoints/checkpoint_10/best_program_info.json (real example showing `iteration`=5, `current_iteration`=10 divergence)
  </read_first>
  <action>
    Replace the body of `_extract_iterations(output_dir, best_info, baseline_accesses, explanations=None)` in `openevolve/consolidate_results.py`. Keep the existing signature unchanged (D-04). The `best_info` parameter becomes unused by row building but stays in the signature for backward compatibility with the call site at lines 87-89 — do not delete it.

    New body behavior:
    1. Compute `checkpoints_dir = os.path.join(output_dir, "checkpoints")`. If `os.path.isdir(checkpoints_dir)` is False, return `[]` (no fallback to best/).
    2. List all entries in `checkpoints_dir` whose names start with `"checkpoint_"` and are directories.
    3. Sort by integer suffix: `dirs.sort(key=lambda d: int(d.replace("checkpoint_", "")))`. Per Pitfall 1 this avoids lexicographic ordering bugs (checkpoint_10 before checkpoint_5).
    4. For each `ckpt_dir`:
       - Compute `checkpoint_n = int(ckpt_dir.replace("checkpoint_", ""))`.
       - Read `best_program_info.json` and `best_program.c` from inside the checkpoint dir.
       - If either file is missing, print `f"Warning: skipping incomplete checkpoint {ckpt_dir}"` to stderr and `continue`. Wrap reads in `try/except (IOError, json.JSONDecodeError)` and skip with a warning on parse failure (re-use the error-handling style at lines 162-165).
       - Read `metrics = info.get("metrics", {})`; `mem_score = metrics.get("mem_score") or metrics.get("combined_score") or 1.0`.
       - `best_found_at = info.get("iteration", checkpoint_n)` — per D-05 this is the JSON `iteration` field (best-found generation), NOT `current_iteration` (Pitfall 1).
       - Derive: `memory_accesses = int(baseline_accesses / mem_score) if mem_score > 0 else baseline_accesses`; `improvement_percent = (baseline_accesses - memory_accesses) / baseline_accesses * 100`; `memory_reads = memory_accesses // 2`; `memory_writes = memory_accesses - memory_reads`.
       - Build the row dict with these keys (order doesn't matter; values do): `"iteration": checkpoint_n` (backward-compat alias per Pitfall 4 so `load_results` sort keeps working), `"checkpoint_iteration": checkpoint_n` (D-05), `"best_found_at_iteration": best_found_at` (D-05), `"code": <code string read from best_program.c>` (CKPT-02), `"combined_score": round(mem_score, 4)` (D-02), `"time_score": None` (D-01), `"memory_accesses": memory_accesses`, `"memory_reads": memory_reads`, `"memory_writes": memory_writes`, `"improvement_percent": round(improvement_percent, 2)`, `"mem_score": round(mem_score, 4)`. Do NOT include `iteration_runtime_seconds` for checkpoint rows (Anti-pattern note in PATTERNS.md: `runtime_seconds` is a Phase 1 placeholder, not real per-checkpoint timing).
       - If `explanations` is truthy and `checkpoint_n in explanations`, attach `row["explanation"] = explanations[checkpoint_n]` only when the value is not None. The key is the integer folder N (Pitfall 2 — never use JSON `iteration` as key).
    5. Append each row to a list and return it.

    Add `import sys` at the top of the file if not already imported (needed for `print(..., file=sys.stderr)`).

    Do NOT change `consolidate_experiment()` body, `_load_best_result()`, `_extract_llm_model()`, or `_build_explanation_config()`. The container key in the returned dict stays `"iterations"` (D-03) — that lives in `consolidate_experiment` line 127 and is unchanged.

    Note on Open Question 1 from RESEARCH.md: `_load_best_result()` is still called by `consolidate_experiment` line 74; leave that call in place (non-breaking). Convergence metrics at lines 91-98 are derived from the `iterations` list via `min(... key=lambda x: x["memory_accesses"])`, which works unchanged because the new rows still have `memory_accesses` and `iteration`.
  </action>
  <verify>
    <automated>cd /home/jolilius/home/src/cavitydetection &amp;&amp; ../openevolve/.venv/bin/python -c "from openevolve.consolidate_results import _extract_iterations; import inspect; src = inspect.getsource(_extract_iterations); assert 'checkpoints' in src, 'no checkpoints traversal'; assert 'checkpoint_iteration' in src, 'missing checkpoint_iteration field'; assert 'time_score' in src, 'missing time_score field'; assert 'combined_score' in src, 'missing combined_score field'; assert 'best_found_at_iteration' in src, 'missing best_found_at_iteration field'; assert 'code' in src, 'missing code field'; assert 'iteration_runtime_seconds' not in src, 'should not write iteration_runtime_seconds for checkpoints'; print('OK')" 2&gt;/dev/null || cd /home/jolilius/home/src/cavitydetection &amp;&amp; ../openevolve/.venv/bin/python -c "import sys; sys.path.insert(0, 'openevolve'); from consolidate_results import _extract_iterations; import inspect; src = inspect.getsource(_extract_iterations); assert 'checkpoints' in src; assert 'checkpoint_iteration' in src; assert 'time_score' in src; assert 'combined_score' in src; assert 'best_found_at_iteration' in src; assert 'code' in src; assert 'iteration_runtime_seconds' not in src; print('OK')"</automated>
    <automated>cd /home/jolilius/home/src/cavitydetection &amp;&amp; ../openevolve/.venv/bin/python openevolve/test_consolidation.py 2&gt;&amp;1 | grep -E "(test_checkpoint_based_consolidation passed|test_checkpoint_explanation_threading passed)"</automated>
  </verify>
  <acceptance_criteria>
    - `_extract_iterations` signature remains `(output_dir, best_info, baseline_accesses, explanations=None) -> list`.
    - Source contains a sorted scan of `checkpoints/checkpoint_N/` (regex/string presence verified above).
    - Source does NOT write `iteration_runtime_seconds` into checkpoint rows.
    - Source assigns `"time_score": None` (D-01).
    - Source assigns `"combined_score": round(mem_score, 4)` (D-02).
    - Source assigns both `"iteration"` and `"checkpoint_iteration"` to the same `checkpoint_n` value (Pitfall 4).
    - Source uses `info.get("iteration", checkpoint_n)` for `best_found_at_iteration` (D-05, Pitfall 1) — NOT `info.get("current_iteration")`.
    - Running `test_consolidation.py`: `test_checkpoint_based_consolidation` and `test_checkpoint_explanation_threading` print their `passed` lines.
    - Existing tests `test_consolidate_with_explanations`, `test_consolidate_without_explanations`, `test_consolidate_partial_explanations` may fail at this point because they build a `best/` fixture (no `checkpoints/` dir) — that is EXPECTED. Do NOT modify those tests to fix the failure; document this in your task summary. (Task 3 follow-up below will keep them as documenting the now-removed code path; the legacy regeneration plan handles the migration story.)
    - On the existing real baseline data: `../openevolve/.venv/bin/python openevolve/consolidate_results.py openevolve/openevolve_output/baseline` runs without exception and prints `Iterations: 12` (12 real checkpoint dirs exist).
  </acceptance_criteria>
  <done>`_extract_iterations` scans `checkpoints/checkpoint_N/` numerically and emits rows with all six Phase 4 schema fields plus the backward-compat `iteration` alias. New Phase 4 tests pass when invoked directly; the public `consolidate_experiment()` signature is unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Extend load_results() column lists in results_loader.py + reconcile legacy best/ tests</name>
  <files>openevolve/results_loader.py, openevolve/test_consolidation.py</files>
  <read_first>
    - openevolve/results_loader.py (full file — column blocks at lines 142-156 and 171-189)
    - openevolve/test_consolidation.py (existing best/-fixture tests at lines 150-257 that will fail after Task 2)
    - .planning/phases/04-per-step-data-pipeline/04-PATTERNS.md (`openevolve/results_loader.py — extend load_results() column lists`)
    - .planning/phases/04-per-step-data-pipeline/04-RESEARCH.md (Pattern 4 + Pitfall 4)
  </read_first>
  <action>
    Two changes:

    (A) In `openevolve/results_loader.py::load_results()`:

    Update the empty-DataFrame column list (currently lines 142-156) to additionally include `checkpoint_iteration`, `best_found_at_iteration`, `code`, `combined_score`, `time_score`. The `iteration` column stays (backward-compat alias).

    Update the ordered column blocks (currently lines 171-185):
    - `primary_cols`: prepend `"checkpoint_iteration"` before `"iteration"`; append `"combined_score"` and `"time_score"` after `"mem_score"`. Final ordering: `["checkpoint_iteration", "iteration", "prompt", "memory_accesses", "improvement_percent", "mem_score", "combined_score", "time_score"]`.
    - `secondary_cols`: prepend `"best_found_at_iteration"` and `"code"` before the existing entries. Final: `["best_found_at_iteration", "code", "memory_reads", "memory_writes", "iteration_runtime_seconds"]`.
    - `explanation_cols` and `metadata_cols`: unchanged.

    The existing guard `existing_cols = [c for c in primary_cols + secondary_cols + explanation_cols + metadata_cols if c in df.columns]` (line ~188) already filters out missing columns, so Phase 1/2 results without the new fields keep loading without KeyError. Do NOT add a separate backward-compat branch.

    The sort at line 192 (`df.sort_values("iteration")`) is unchanged — it works because Task 2 emits `"iteration": checkpoint_n` as alias (Pitfall 4).

    (B) In `openevolve/test_consolidation.py`:

    The tests `test_consolidate_with_explanations` (line 150), `test_consolidate_without_explanations` (line 186), and `test_consolidate_partial_explanations` (line 219) construct only a `best/` fixture and expect `consolidate_experiment` to produce a single row. After Task 2 these will fail because `_extract_iterations` no longer falls back to `best/`. Convert each of these three tests by replacing the `best_dir` fixture with a single-checkpoint `checkpoints/checkpoint_<N>/` fixture that contains `best_program_info.json` and `best_program.c`, where `<N>` matches the iteration number the original test asserts on (25, 42, 5, respectively). For example, the original `synthetic_best` dict with `"iteration": 42` becomes a folder `checkpoints/checkpoint_42/` whose `best_program_info.json` is `{"iteration": 42, "current_iteration": 42, "metrics": {"mem_score": 1.25}}` plus a short `best_program.c`. The assertions on `result['iterations'][0].get('explanation')` and `result_dict[5]` keep working because `iteration` (alias) still equals the checkpoint folder N. Do NOT change `test_load_results_with_explanations` or `test_load_phase1_results` — they build `results.json` directly without `consolidate_experiment` and remain valid.

    Also: update `test_load_results_with_explanations` (line 260) and `test_load_phase1_results` (line 327) ONLY to extend their column-existence assertions to include the new fields when the underlying JSON has them — but their JSON fixtures don't include the new fields, so the existing column guard means assertions like `'explanation' in df.columns` continue to work. Do NOT add new asserts to those two tests unless required to keep them passing.

    Finally, ensure `test_checkpoint_load_results` (from Task 1) now passes — this is the dedicated test for the new columns.
  </action>
  <verify>
    <automated>cd /home/jolilius/home/src/cavitydetection &amp;&amp; ../openevolve/.venv/bin/python -c "import ast; src = open('openevolve/results_loader.py').read(); assert '\"checkpoint_iteration\"' in src; assert '\"best_found_at_iteration\"' in src; assert '\"code\"' in src; assert '\"combined_score\"' in src; assert '\"time_score\"' in src; print('OK: all 5 new columns referenced in results_loader.py')"</automated>
    <automated>cd /home/jolilius/home/src/cavitydetection &amp;&amp; ../openevolve/.venv/bin/python openevolve/test_consolidation.py</automated>
  </verify>
  <acceptance_criteria>
    - `openevolve/results_loader.py` string-contains all of `"checkpoint_iteration"`, `"best_found_at_iteration"`, `"code"`, `"combined_score"`, `"time_score"` (literal occurrences in column lists).
    - The empty-DataFrame branch column list (lines ~142-156 region) includes all five new columns plus the existing `iteration`.
    - `primary_cols` first element is `"checkpoint_iteration"` (lexically before `"iteration"`).
    - `secondary_cols` includes `"best_found_at_iteration"` and `"code"` ahead of `"memory_reads"`.
    - Running `../openevolve/.venv/bin/python openevolve/test_consolidation.py` exits with status 0 and prints `"✅ All explanation tests passed"` plus `"test_checkpoint_based_consolidation passed"`, `"test_checkpoint_load_results passed"`, `"test_checkpoint_explanation_threading passed"`.
    - The three converted best/-fixture tests (`test_consolidate_with_explanations`, `test_consolidate_without_explanations`, `test_consolidate_partial_explanations`) now use `checkpoints/checkpoint_N/` fixtures and still print their `passed` lines.
    - Real-data smoke check: `../openevolve/.venv/bin/python -c "import sys; sys.path.insert(0, 'openevolve'); from consolidate_results import consolidate_experiment; from results_loader import load_results; consolidate_experiment('openevolve/openevolve_output/baseline'); df = load_results('openevolve/openevolve_output/baseline/results.json'); print(len(df), 'rows'); cols = set(df.columns); assert {'checkpoint_iteration', 'best_found_at_iteration', 'code', 'combined_score', 'time_score'} <= cols; print('OK')"` prints `12 rows` and `OK`.
  </acceptance_criteria>
  <done>`load_results()` exposes all five new Phase 4 columns; the full `test_consolidation.py` suite (six original + three new tests) passes end-to-end; running consolidation on the real `baseline/` produces a 12-row DataFrame with all Phase 4 columns.</done>
</task>

</tasks>

<verification>
After all three tasks:
- `../openevolve/.venv/bin/python openevolve/test_consolidation.py` exits 0, prints all 9 `passed` lines.
- Consolidating real data: `../openevolve/.venv/bin/python openevolve/consolidate_results.py openevolve/openevolve_output/baseline` writes a `results.json` whose `iterations` list has 12 entries (one per real checkpoint dir).
- Manually inspect: `head -c 400 openevolve/openevolve_output/baseline/results.json | python -m json.tool | head -40` shows the first iteration row containing `checkpoint_iteration`, `best_found_at_iteration`, `code`, `combined_score`, `time_score: null`.
- `load_results` on the real file returns a DataFrame whose `code` column entries each contain literal C source text (e.g. `df.iloc[0]['code'].count('void GaussBlur') >= 1`).
</verification>

<success_criteria>
- CKPT-01: results.json built from `checkpoints/checkpoint_N/` sorted numerically — verified by `test_checkpoint_based_consolidation` asserting `[5, 10, 15]` order.
- CKPT-02: Each row has `checkpoint_iteration`, `best_found_at_iteration`, `code`, `combined_score`, `mem_score`, `time_score` — verified by direct key-presence asserts in the new tests.
- CKPT-03: `load_results()` exposes those columns plus existing ones in a DataFrame with one row per checkpoint — verified by `test_checkpoint_load_results` and the real-data smoke check.
- D-01, D-02, D-03, D-04, D-05 honored (verified by source assertions in acceptance criteria).
- Pitfall 1, 2, 4 actively avoided (verified by tests for divergent `iteration`/`current_iteration`, integer-keyed explanations dict, and surviving `iteration` alias).
- Existing 6 tests in `test_consolidation.py` continue to pass after migration to checkpoint fixtures.
</success_criteria>

<output>
After completion, create `.planning/phases/04-per-step-data-pipeline/04-01-SUMMARY.md` capturing: which tests pass, the schema of a row in the regenerated `baseline/results.json` (paste one), and any deviations from the plan (especially around the best/-fixture test conversion in Task 3).
</output>
