# OpenEvolve Results & Explanations Framework — Requirements Archive

**Milestone:** v1.1 — Experiment Runs + Per-Step Analysis
**Archived:** 2026-05-15
**Status:** ✅ All 10 v1.1 requirements satisfied

---

## Research Goal

Understand how different prompts influence OpenEvolve's search behavior:
- How quickly does each prompt converge to an optimum?
- How good is the optimum for each prompt?
- What is the LLM thinking at each iteration?

## v1.0 Requirements (COMPLETE — archived)

### Phase 1: Results Consolidation
- [x] Unified results storage: single consolidated JSON per experiment run — **v1.0**
- [x] Results schema: prompt, iteration, code, memory_reads, memory_writes — **v1.0**
- [x] Results loader: `load_results()` / `load_all_results()` returning pandas DataFrame — **v1.0**
- [x] `run_experiment.py` outputs consolidated format — **v1.0**
- [x] `make evolve-all` aggregates results — **v1.0**

### Phase 2: LLM Explanations
- [x] Per-iteration LLM explanation stored alongside results — **v1.0**
- [x] `explanation` field in consolidated results — **v1.0**
- [x] `get_explanations()` utility in results loader — **v1.0**

---

## v1.1 Requirements (COMPLETE)

### Clarified Research Goal

The primary research objective is to **observe the LLM's reasoning trajectory across the full evolution**, not just the final result. Each checkpoint is a decision point: what did the model change, and why? The system must make these intermediate steps first-class data — both the code at each step and an explanation of what changed.

### Run Organization

- [x] **RUNORG-01:** Run name auto-generates from timestamp + model name; overridable with `--run` — **v1.1 / Phase 3** *(verified: VERIFICATION.md 13/13, test_run_structure.py 10/10)*
- [x] **RUNORG-02:** `make evolve-all` creates one run directory; all prompts share the same run ID — **v1.1 / Phase 3** *(verified: test_evolve_all_shared_run)*
- [x] **RUNORG-03:** Each run directory contains `metadata.json` with model, iterations, programs, prompts, config snapshot, start timestamp — **v1.1 / Phase 3** *(verified: test_metadata_fields)*

### Legacy Migration

- [x] **MIGRATE-01:** Migration script (`migrate_legacy.py`) moves legacy dirs to `runs/legacy/cavitydetection/` with no data loss — **v1.1 / Phase 3** *(code complete and tested; actual migration pending manual run)*

### Checkpoint-Based Consolidation

- [x] **CKPT-01:** `results.json` built from `checkpoints/checkpoint_N/` directories sorted numerically — **v1.1 / Phase 4** *(verified: test_checkpoint_based_consolidation)*
- [x] **CKPT-02:** Each checkpoint row includes `checkpoint_iteration`, `best_found_at_iteration`, `code`, `combined_score`, `mem_score`, `time_score` — **v1.1 / Phase 4** *(verified: key-presence asserts + real 12-row smoke test)*
- [x] **CKPT-03:** `load_results()` returns DataFrame with one row per checkpoint; `load_all_results()` aggregates across runs/programs/prompts — **v1.1 / Phase 4** *(verified: test_checkpoint_load_results)*

### Per-Step Explanations

- [x] **EXPLAIN-01:** One explanation per checkpoint comparing to previous checkpoint (first compares to `initial_program.c`) — **v1.1 / Phase 4** *(verified: sliding-window synthetic test + live UAT 2026-05-15)*
- [x] **EXPLAIN-02:** Explanations stored in `results.json` per checkpoint row, exposed as `df['explanation']` — **v1.1 / Phase 4** *(verified: test_checkpoint_explanation_threading + live UAT 2026-05-15)*

### Display & Access

- [x] **DISPLAY-01:** `make show-results` and `make show-consolidated-results` work with run structure; accept optional `RUN=<id>` — **v1.1 / Phase 3** *(verified: test_show_results_run_filter, test_show_consolidated_run_filter)*

---

## Traceability

| REQ-ID | Phase | Final Status | Evidence |
|--------|-------|-------------|----------|
| RUNORG-01 | Phase 3 | ✅ SATISFIED | VERIFICATION.md (13/13), test_run_structure.py 03-01 |
| RUNORG-02 | Phase 3 | ✅ SATISFIED | VERIFICATION.md (13/13), test_evolve_all_shared_run |
| RUNORG-03 | Phase 3 | ✅ SATISFIED | VERIFICATION.md (13/13), test_metadata_fields |
| MIGRATE-01 | Phase 3 | ✅ SATISFIED | Code complete + tested; real data migration pending |
| DISPLAY-01 | Phase 3 | ✅ SATISFIED | VERIFICATION.md (13/13), display filter tests |
| CKPT-01 | Phase 4 | ✅ SATISFIED | VERIFICATION.md + test_checkpoint_based_consolidation |
| CKPT-02 | Phase 4 | ✅ SATISFIED | VERIFICATION.md + real 12-row smoke test |
| CKPT-03 | Phase 4 | ✅ SATISFIED | VERIFICATION.md + test_checkpoint_load_results |
| EXPLAIN-01 | Phase 4 | ✅ SATISFIED | VERIFICATION.md + live UAT 2026-05-15 |
| EXPLAIN-02 | Phase 4 | ✅ SATISFIED | VERIFICATION.md + test_checkpoint_explanation_threading + live UAT |

**Note on traceability checkboxes:** The original REQUIREMENTS.md traceability table showed all `[ ]` — checkboxes were never updated during execution. This was a documentation gap. All 10 requirements are confirmed satisfied by cross-referencing VERIFICATION.md (3-source audit: VERIFICATION.md × SUMMARY frontmatter × REQUIREMENTS.md), documented in `v1.1-MILESTONE-AUDIT.md`.

---

## Out of Scope for v1.1

- Multi-program support (deferred to v1.2+)
- Theme grouping of explanations (dropped — motivation unclear)
- Metric plugins (valgrind, energy)
- Web dashboards or visualization

---

_For current requirements, see .planning/REQUIREMENTS.md (next milestone)_
