---
phase: 04-per-step-data-pipeline
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - openevolve/run_experiment.py
autonomous: true
requirements:
  - EXPLAIN-01
  - EXPLAIN-02

must_haves:
  truths:
    - "When EXPLAIN_GENERATIONS != 0, run_experiment.py generates one explanation per checkpoint directory, in numeric order"
    - "Checkpoint 0 (the lowest-N checkpoint, typically checkpoint_5) compares its best_program.c to initial_program.c"
    - "Checkpoint N>0 compares its best_program.c to the previous checkpoint's best_program.c (sliding window)"
    - "The explanations dict returned to consolidate_experiment() is keyed by integer folder N — same key space as _extract_iterations() expects"
    - "When EXPLAIN_GENERATIONS=0 the explanation loop is skipped and every row's explanation field is absent/None"
    - "D-06: Explanation generation runs at the END of run_experiment.py main() after OpenEvolve finishes writing checkpoints, not mid-loop"
    - "D-07: EXPLAIN_GENERATIONS=0 (existing Phase 2 env var) disables all checkpoint explanations; no new env var or flag introduced"
    - "D-08: Each checkpoint explanation uses a sliding window — checkpoint N compares to checkpoint N-1's best_program.c; the first checkpoint compares to initial_program.c (the baseline_code argument)"
  artifacts:
    - path: "openevolve/run_experiment.py"
      provides: "Per-checkpoint explanation loop replacing best/-only explanation"
      contains: "checkpoint_"
  key_links:
    - from: "openevolve/run_experiment.py::_generate_explanations_for_experiment"
      to: "openevolve/explanation_generator.py::generate_explanation"
      via: "per-checkpoint sliding-window call with baseline_code = prev_code"
      pattern: "generate_explanation\\("
    - from: "openevolve/run_experiment.py (call site near line 243)"
      to: "openevolve/consolidate_results.py::_extract_iterations"
      via: "explanations dict keyed by checkpoint folder N"
      pattern: "explanations\\[n\\]"
---

<objective>
Replace the body of `_generate_explanations_for_experiment()` in `run_experiment.py` with a per-checkpoint loop that walks `checkpoints/checkpoint_N/` in numeric order and generates one LLM explanation per checkpoint, comparing each checkpoint's `best_program.c` to the previous checkpoint's (or to `initial_program.c` for the first checkpoint, per D-08).

Purpose: Satisfy EXPLAIN-01 (one explanation per checkpoint, delta vs previous) and EXPLAIN-02 (explanations land in `results.json` rows and surface as `df['explanation']`).

Output: Updated `run_experiment.py` where `_generate_explanations_for_experiment` returns a dict keyed by integer folder N. The existing call site at lines 237-256 (env-var gate, exception handling, pass-through to `consolidate_experiment(... explanations=explanations ...)`) is unchanged.
</objective>

<execution_context>
@/home/jolilius/.claude/get-shit-done/workflows/execute-plan.md
@/home/jolilius/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-per-step-data-pipeline/04-CONTEXT.md
@.planning/phases/04-per-step-data-pipeline/04-RESEARCH.md
@.planning/phases/04-per-step-data-pipeline/04-PATTERNS.md
@.planning/phases/04-per-step-data-pipeline/04-01-SUMMARY.md
@openevolve/run_experiment.py
@openevolve/explanation_generator.py

<interfaces>
<!-- Key contracts the executor needs. -->

From `openevolve/explanation_generator.py` (UNCHANGED — call it, do not modify):
- `generate_explanation(evolved_code: str, baseline_code: str, llm_config: dict, explanation_prompt_text: str) -> Optional[str]` — returns a 1–2 sentence string or `None` on any failure (timeout, HTTP error, parse error, empty response). No need to wrap in try/except for the LLM call itself.

From `openevolve/run_experiment.py` (CHANGE INTERNALS of):
- Function `_generate_explanations_for_experiment(output_dir: str, baseline_code: str, llm_config: dict, explanation_prompt: str) -> dict` (lines 47-114). Signature unchanged. Body replaced.
- Call site at lines 237-256 inside `main()`: unchanged. The env-var gate `EXPLAIN_GENERATIONS != "0"` (D-07) is already in place. The call passes the result dict to `consolidate_experiment(... explanations=explanations ...)` on line 265.

From Plan 04-01 (depends_on):
- `_extract_iterations()` in `consolidate_results.py` now reads `explanations[checkpoint_n]` where `checkpoint_n` is the integer folder number. The dict produced here MUST use the same integer key space.

Real data shape (from `openevolve_output/baseline/checkpoints/`):
- 12 checkpoint directories: `checkpoint_5, checkpoint_10, ..., checkpoint_80` (gap between 55 and 80 — confirms checkpoints are NOT guaranteed to be evenly spaced).
- Sort key: `int(d.replace("checkpoint_", ""))`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite _generate_explanations_for_experiment() body to iterate checkpoints</name>
  <files>openevolve/run_experiment.py</files>
  <read_first>
    - openevolve/run_experiment.py (full file — current implementation lines 47-114, call site lines 237-256)
    - openevolve/explanation_generator.py (just confirm `generate_explanation` signature and that it returns Optional[str])
    - .planning/phases/04-per-step-data-pipeline/04-CONTEXT.md (decisions D-06, D-07, D-08)
    - .planning/phases/04-per-step-data-pipeline/04-RESEARCH.md (Pattern 2 + Pitfall 2)
    - .planning/phases/04-per-step-data-pipeline/04-PATTERNS.md (`openevolve/run_experiment.py — replace _generate_explanations_for_experiment() internals`)
  </read_first>
  <action>
    In `openevolve/run_experiment.py`, replace the body of `_generate_explanations_for_experiment(output_dir, baseline_code, llm_config, explanation_prompt)` (currently lines 47-114). Keep the function name, the four-parameter signature, the docstring intent (update wording to say "per-checkpoint" instead of "for best solution"), and the `if generate_explanation is None: return {}` early-return at the top.

    New body behavior (D-06, D-07, D-08):
    1. After the `generate_explanation is None` guard, compute `checkpoints_dir = os.path.join(output_dir, "checkpoints")`. If `not os.path.isdir(checkpoints_dir)`, print warning to stderr (`f"Warning: no checkpoints/ directory at {output_dir}, skipping explanations"`) and return `{}`.
    2. List entries in `checkpoints_dir` whose names start with `"checkpoint_"` and are directories. If empty, return `{}`.
    3. Sort by integer suffix using the same key as Plan 04-01: `key=lambda d: int(d.replace("checkpoint_", ""))`.
    4. Initialize `prev_code = baseline_code` — per D-08, the first checkpoint (lowest N, typically 5) compares against `initial_program.c`, which the caller passes as `baseline_code` (see line 245-247 of the call site).
    5. Initialize `explanations = {}`.
    6. For each `ckpt_dir` in the sorted list:
       - Compute `n = int(ckpt_dir.replace("checkpoint_", ""))` — this is the integer key the dict will use (Pitfall 2: NOT `info["iteration"]`).
       - Compute `code_path = os.path.join(checkpoints_dir, ckpt_dir, "best_program.c")`.
       - If `not os.path.isfile(code_path)`: print `f"Warning: no best_program.c in {ckpt_dir}, skipping"` to stderr and `continue` (do NOT advance `prev_code` since this checkpoint contributed nothing; the next valid checkpoint compares against the same previous code).
       - Wrap the file read in `try/except (IOError, OSError) as e:` with a warning + continue.
       - Read the file into `evolved_code`.
       - Print progress to stderr: `f"Generating explanation for checkpoint {n}..."`.
       - Call `explanation = generate_explanation(evolved_code=evolved_code, baseline_code=prev_code, llm_config=llm_config, explanation_prompt_text=explanation_prompt)` — note the keyword args, baseline_code is PREVIOUS checkpoint's code (D-08, sliding window).
       - Store `explanations[n] = explanation`. The value may be `None` — this is acceptable per D-07 / Pitfall 2; consolidate_results.py only attaches non-None values to rows (see Task 2 of Plan 04-01).
       - If `explanation` is truthy, print `f"  checkpoint_{n}: {explanation[:80]}..."` to stderr (matches existing style at line 112).
       - Advance the sliding window: `prev_code = evolved_code` (only after a successful read; not after skip — Pitfall 2 reasoning).
    7. Return `explanations`.

    Do NOT touch the call site at lines 237-256. Do NOT touch `main()` argument parsing, the `subprocess.run` call, or the `consolidate_experiment()` call at line 260. Do NOT change the `EXPLAIN_GENERATIONS` env-var check at line 238 — D-07 explicitly reuses the existing flag, no new flag.

    Imports: `os`, `sys`, `json` are already imported at the top of the file. No new imports required.

    Remove the now-dead lines that read `best/best_program_info.json`, extract `evolved_code` from `program_source`/`evolved_code` JSON fields, and `iteration_num = best_info.get("iteration", 1)` (these are all replaced by the checkpoint loop).
  </action>
  <verify>
    <automated>cd /home/jolilius/home/src/cavitydetection &amp;&amp; ../openevolve/.venv/bin/python -c "import ast, sys; sys.path.insert(0, 'openevolve'); src = open('openevolve/run_experiment.py').read(); tree = ast.parse(src); fn = [n for n in tree.body if isinstance(n, ast.FunctionDef) and n.name == '_generate_explanations_for_experiment'][0]; body_src = ast.unparse(fn); assert 'checkpoints' in body_src, 'missing checkpoint traversal'; assert 'prev_code' in body_src or 'baseline_code' in body_src, 'missing sliding window var'; assert 'best/best_program_info.json' not in body_src, 'still reads from best/'; assert 'program_source' not in body_src, 'still references program_source field'; print('OK')"</automated>
    <automated>cd /home/jolilius/home/src/cavitydetection &amp;&amp; EXPLAIN_GENERATIONS=0 ../openevolve/.venv/bin/python -c "import sys; sys.path.insert(0, 'openevolve'); from run_experiment import _generate_explanations_for_experiment; import tempfile, os; d = tempfile.mkdtemp(); os.makedirs(os.path.join(d, 'checkpoints')); result = _generate_explanations_for_experiment(d, 'baseline code', {}, 'prompt'); assert result == {}, f'expected empty dict for no checkpoints, got {result}'; print('OK empty')"</automated>
    <automated>cd /home/jolilius/home/src/cavitydetection &amp;&amp; ../openevolve/.venv/bin/python -c "
import sys, os, tempfile, json
sys.path.insert(0, 'openevolve')
# Stub generate_explanation so we don't need a live LLM
import explanation_generator
def fake_gen(evolved_code, baseline_code, llm_config, explanation_prompt_text):
    return f'EXPL({len(evolved_code)} vs {len(baseline_code)})'
explanation_generator.generate_explanation = fake_gen
import importlib, run_experiment
run_experiment.generate_explanation = fake_gen
d = tempfile.mkdtemp()
for n, body in [(5, 'A'*10), (10, 'B'*20), (15, 'C'*30)]:
    p = os.path.join(d, 'checkpoints', f'checkpoint_{n}')
    os.makedirs(p)
    with open(os.path.join(p, 'best_program.c'), 'w') as f: f.write(body)
result = run_experiment._generate_explanations_for_experiment(d, 'INIT'*1, {}, 'prompt')
assert set(result.keys()) == {5, 10, 15}, f'keys should be integer folder Ns, got {result.keys()}'
# checkpoint_5 compares to baseline (len 4), checkpoint_10 compares to checkpoint_5's body (len 10)
assert 'vs 4' in result[5], f'checkpoint 5 should compare to baseline len=4, got {result[5]}'
assert 'vs 10' in result[10], f'checkpoint 10 should compare to checkpoint_5 len=10, got {result[10]}'
assert 'vs 20' in result[15], f'checkpoint 15 should compare to checkpoint_10 len=20, got {result[15]}'
print('OK sliding window')
"</automated>
  </verify>
  <acceptance_criteria>
    - `_generate_explanations_for_experiment` body no longer references `"best/best_program_info.json"` (substring absent).
    - Body no longer reads `"program_source"` or `"evolved_code"` JSON fields (substrings absent — they were Phase 2's path).
    - Body contains the literal string `"checkpoint_"` (the loop iterates checkpoint dirs).
    - Body uses the sort key `int(d.replace("checkpoint_", ""))` (or equivalent numeric extraction; verified by AST/source contains check).
    - The third automated check above (stubbed `generate_explanation`) passes, proving: dict keys are integers (5, 10, 15); sliding window advances correctly (checkpoint_10 compares to checkpoint_5's code, NOT to baseline); skip behavior on missing files doesn't crash.
    - `EXPLAIN_GENERATIONS=0 ../openevolve/.venv/bin/python openevolve/test_consolidation.py` exits 0 (existing tests still pass; this plan does not regress Plan 04-01).
    - The call site lines 237-256 in `main()` are unchanged (verify by reading; no edits should appear there).
    - `consolidate_experiment` call at line 260 still receives `explanations=explanations` exactly as before (no signature change).
  </acceptance_criteria>
  <done>`_generate_explanations_for_experiment` walks checkpoints in numeric order, generates per-checkpoint explanations with a sliding-window baseline (D-08), returns a dict keyed by integer folder N, and the result threads correctly through `consolidate_experiment` to land in `df['explanation']`. EXPLAIN_GENERATIONS=0 path skips the loop entirely.</done>
</task>

<task type="auto">
  <name>Task 2: End-to-end smoke test on real baseline data (EXPLAIN_GENERATIONS=0)</name>
  <files>openevolve/run_experiment.py</files>
  <read_first>
    - openevolve/run_experiment.py (just to confirm Task 1's changes are in place)
    - openevolve/openevolve_output/baseline/checkpoints/ (verify 12 checkpoint dirs exist)
  </read_first>
  <action>
    No code change. This task is a verification-only checkpoint: run the consolidation+loader pipeline on the real baseline experiment with explanations disabled (EXPLAIN_GENERATIONS=0) to confirm the post-Phase-4 pipeline reads real checkpoint data without crashing and the explanation column is absent (or NaN) rather than populated.

    Specifically:
    1. Confirm Task 1's body is in place (re-run the AST check from Task 1).
    2. Invoke the pipeline directly without spawning a real `openevolve-run.py` (which would re-run an experiment): call `consolidate_experiment` on `openevolve/openevolve_output/baseline` with `explanations={}` (empty), then `load_results` on the produced `results.json`.
    3. Verify: the DataFrame has 12 rows; `df['explanation'].isna().all()` is True (no explanations were threaded); `df['code'].str.len().min() > 100` (code column has substantial content from real checkpoints — real `best_program.c` files are ~10KB).

    The "task" is to produce a short verification report (in the task summary text returned to the user) that shows the row count, column names, and the first 60 chars of `df.iloc[0]['code']`.
  </action>
  <verify>
    <automated>cd /home/jolilius/home/src/cavitydetection &amp;&amp; ../openevolve/.venv/bin/python -c "
import sys; sys.path.insert(0, 'openevolve')
from consolidate_results import consolidate_experiment
from results_loader import load_results
consolidate_experiment('openevolve/openevolve_output/baseline', explanations={})
df = load_results('openevolve/openevolve_output/baseline/results.json')
assert len(df) == 12, f'expected 12 checkpoint rows, got {len(df)}'
assert df['explanation'].isna().all(), 'explanations should all be NaN when explanations={} passed'
assert df['code'].str.len().min() > 100, f'code column min length suspicious: {df[\"code\"].str.len().min()}'
assert df['time_score'].isna().all(), 'time_score should always be null'
print(f'OK rows={len(df)} code_min_len={df[\"code\"].str.len().min()} cols={sorted(df.columns.tolist())}')
"</automated>
  </verify>
  <acceptance_criteria>
    - Smoke check above prints `OK rows=12` and a code length >= ~10000 chars (real C source size).
    - The printed column list contains `checkpoint_iteration`, `best_found_at_iteration`, `code`, `combined_score`, `mem_score`, `time_score`, `iteration`, `prompt`.
    - `time_score` column is all NaN.
    - `explanation` column is all NaN (since no explanations were passed).
    - No exception raised, no warnings about missing files (real baseline has complete checkpoints).
    - Task summary records: row count, code length range across the 12 rows, distinct `best_found_at_iteration` values (expected: most rows have `best_found_at_iteration == 5` per the RESEARCH.md observation).
  </acceptance_criteria>
  <done>Real baseline data produces a 12-row DataFrame with all Phase 4 columns, no crashes, no spurious explanation values. The explanation generation path is dormant when no explanations are threaded.</done>
</task>

</tasks>

<verification>
- Synthetic sliding-window check (Task 1 third verify command) confirms D-08 correctness on Python data without needing the LLM.
- Real-data smoke (Task 2) confirms the pipeline handles 12 real checkpoint dirs correctly.
- Test suite from Plan 04-01 (`../openevolve/.venv/bin/python openevolve/test_consolidation.py`) still passes after Plan 04-02 changes.
- Manual / out-of-scope: live LLM end-to-end test with `EXPLAIN_GENERATIONS=1 ../openevolve/.venv/bin/python openevolve/run_experiment.py baseline --iterations 5` — flagged in VALIDATION.md Manual-Only Verifications table. Not required for plan completion.
</verification>

<success_criteria>
- EXPLAIN-01: One explanation generated per checkpoint, comparing to previous checkpoint (or initial_program.c for the first). Verified by synthetic sliding-window assertion.
- EXPLAIN-02: Explanations stored in results.json per checkpoint row and exposed as `df['explanation']`. Verified by end-to-end test through `test_checkpoint_explanation_threading` (defined in Plan 04-01, now genuinely exercising the explanation path because `consolidate_experiment` accepts the integer-keyed dict produced by this plan's function).
- D-06: Explanation generation runs post-OpenEvolve, at the end of `run_experiment.py` `main()` — preserved by NOT moving the call site.
- D-07: `EXPLAIN_GENERATIONS=0` disables explanations — preserved by NOT modifying the env-var check at line 238.
- D-08: Sliding-window baseline (checkpoint N compares to N-1; checkpoint 0 compares to initial_program.c) — verified by synthetic test.
- Pitfall 2 avoided: dict keys are integer folder Ns matching `_extract_iterations()`'s lookup key — verified by `set(result.keys()) == {5, 10, 15}` assert.
</success_criteria>

<output>
After completion, create `.planning/phases/04-per-step-data-pipeline/04-02-SUMMARY.md` recording: the sliding-window verification output, the real-data smoke check output (12 rows, code length range), and confirmation that the call site / EXPLAIN_GENERATIONS check were left intact.
</output>
