---
phase: 04-per-step-data-pipeline
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - openevolve/test_run_structure.py
  - openevolve/migrate_legacy.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "migrate_legacy.py accepts a --regenerate flag; when passed, it iterates each migrated experiment at runs/legacy/cavitydetection/<name>/ that has a checkpoints/ subdir"
    - "For each such experiment, the existing results.json is copied to results.json.v1 (with metadata) BEFORE consolidate_experiment overwrites it"
    - "Regeneration is opt-in: running migrate_legacy.py without --regenerate only moves directories (existing behavior preserved)"
    - "Regeneration on a tree with no runs/legacy/cavitydetection/ directory prints a friendly message and exits cleanly (no crash)"
    - "Regeneration skips experiment directories that have no checkpoints/ subdir without crashing"
    - "D-09: migrate_legacy.py has a --regenerate flag that is opt-in; running without the flag preserves existing migration-only behavior"
    - "D-10: results.json.v1 backup is always created via shutil.copy2 BEFORE consolidate_experiment writes the new results.json; no overwrite without backup"
  artifacts:
    - path: "openevolve/test_run_structure.py"
      provides: "Failing-then-passing test for --regenerate that asserts results.json.v1 backup is created and results.json is rebuilt from checkpoints"
      contains: "def test_regenerate_flag"
    - path: "openevolve/migrate_legacy.py"
      provides: "regenerate_results() helper + --regenerate CLI flag"
      contains: "def regenerate_results"
    - path: "openevolve/migrate_legacy.py"
      provides: "argparse flag --regenerate"
      contains: "--regenerate"
  key_links:
    - from: "openevolve/migrate_legacy.py::regenerate_results"
      to: "openevolve/consolidate_results.py::consolidate_experiment"
      via: "imported inline (try/except for module vs script context) and called once per experiment dir"
      pattern: "from consolidate_results import consolidate_experiment"
    - from: "openevolve/migrate_legacy.py::regenerate_results"
      to: "shutil.copy2 backup of results.json"
      via: "always-before-overwrite (D-10)"
      pattern: "results\\.json\\.v1"
---

<objective>
Add a `--regenerate` flag to `migrate_legacy.py` (D-09, D-10) so an existing legacy experiment's `results.json` can be rebuilt from its `checkpoints/` directory using the new Plan 04-01 consolidation logic, with `results.json.v1` written as a rollback backup beforehand. Wave 0 test stub goes in first.

Purpose: Provide a one-shot upgrade path for legacy experiments (currently at `openevolve_output/baseline/` and `openevolve_output/prompt1/`, post-migration at `runs/legacy/cavitydetection/<name>/`) without requiring a re-run.

Output: `migrate_legacy.py` with `--regenerate` and `regenerate_results()` helper; `test_run_structure.py` with a new `test_regenerate_flag()` covering D-09 and D-10.
</objective>

<execution_context>
@/home/jolilius/.claude/get-shit-done/workflows/execute-plan.md
@/home/jolilius/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-per-step-data-pipeline/04-CONTEXT.md
@.planning/phases/04-per-step-data-pipeline/04-RESEARCH.md
@.planning/phases/04-per-step-data-pipeline/04-PATTERNS.md
@.planning/phases/04-per-step-data-pipeline/04-VALIDATION.md
@.planning/phases/04-per-step-data-pipeline/04-01-SUMMARY.md
@openevolve/migrate_legacy.py
@openevolve/test_run_structure.py

<interfaces>
<!-- Key contracts the executor needs. -->

From `openevolve/migrate_legacy.py` (UNCHANGED behavior to preserve):
- `migrate_legacy(output_root: str) -> None` (lines 21-49): moves non-runs/ experiment dirs under `output_root/runs/legacy/cavitydetection/`. Idempotent.
- `main()` (lines 52-68): argparse with `--output-root`, then `migrate_legacy(output_root)`.

From `openevolve/consolidate_results.py` (Plan 04-01 dependency):
- `consolidate_experiment(output_dir, program="cavitydetection", prompt_variant=None, baseline_accesses=128_862_705, explanations=None, explanation_prompt_text=None, prompt_version="1.0") -> dict` — writes `output_dir/results.json` as side effect. After Plan 04-01, this scans `output_dir/checkpoints/checkpoint_N/`.

Post-migration directory layout (target tree for `--regenerate` to walk):
```
<output_root>/
  runs/
    legacy/
      cavitydetection/
        baseline/
          checkpoints/checkpoint_N/...
          results.json          (legacy v1 schema — to be backed up + regenerated)
        prompt1/
          checkpoints/checkpoint_N/...
          results.json
```

Pitfall 5 (from RESEARCH.md): `--regenerate` MUST scan `runs/legacy/cavitydetection/*/`, not `output_root` directly, to avoid attempting to regenerate non-experiment directories like `runs/` itself.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Wave 0 failing test_regenerate_flag in test_run_structure.py</name>
  <files>openevolve/test_run_structure.py</files>
  <read_first>
    - openevolve/test_run_structure.py (full file — fixture style at lines 137-187 and 189-218, runner block lines 354-401)
    - .planning/phases/04-per-step-data-pipeline/04-VALIDATION.md (Wave 0 Requirements last bullet)
    - .planning/phases/04-per-step-data-pipeline/04-PATTERNS.md (`openevolve/test_run_structure.py — add --regenerate test`)
    - .planning/phases/04-per-step-data-pipeline/04-CONTEXT.md (decisions D-09, D-10)
  </read_first>
  <action>
    Add a new test function `test_regenerate_flag()` to `openevolve/test_run_structure.py`. Add the import path setup if not already at the top — the file already adds `SCRIPT_DIR` to `sys.path`, so `from migrate_legacy import regenerate_results` works once `regenerate_results` exists.

    Test body:
    1. Use `tempfile.TemporaryDirectory()` as `tmpdir`.
    2. Build the post-migration tree manually (do not call `migrate_legacy()` — we are testing regeneration in isolation):
       - Create `legacy_exp = os.path.join(tmpdir, "runs", "legacy", "cavitydetection", "baseline")`.
       - Inside `legacy_exp`, write an `results.json` containing an obvious legacy-shaped payload, e.g. `json.dump({"metadata": {"prompt_variant": "baseline"}, "iterations": [{"iteration": 1, "memory_accesses": 99999, "mem_score": 1.0}], "baseline_metrics": {"memory_accesses": 128862705}}, f)`. Store this content in a Python variable so we can verify the backup contains it byte-for-byte.
       - Create `checkpoints/checkpoint_5/` and `checkpoints/checkpoint_10/` inside `legacy_exp`. In each, write `best_program_info.json = {"iteration": 5, "current_iteration": <N>, "metrics": {"mem_score": 1.10 if N==5 else 1.15}, "timestamp": "..."}` and `best_program.c = f"/* synthetic checkpoint {N} */"`.
    3. Also build a "no-checkpoints" experiment dir at `runs/legacy/cavitydetection/empty_exp/` containing just an empty `results.json` (no `checkpoints/` subdir) to verify the skip path (Pitfall 5).
    4. Call `regenerate_results(tmpdir)`. The function should NOT raise.
    5. Assertions on `baseline`:
       - `backup_path = os.path.join(legacy_exp, "results.json.v1")` exists (D-10).
       - The backup file content equals the original legacy payload (byte-for-byte comparison via reading both files; the backup is a faithful copy).
       - `os.path.isfile(os.path.join(legacy_exp, "results.json"))` is True (regeneration left it in place).
       - The new `results.json` parses as JSON, top-level key `"iterations"` is a list of length 2 (one per synthetic checkpoint).
       - `data["iterations"][0]["checkpoint_iteration"] == 5` and `data["iterations"][1]["checkpoint_iteration"] == 10`.
       - `data["iterations"][0]["code"]` contains the substring `"synthetic checkpoint 5"`.
       - `data["iterations"][0]["time_score"] is None`.
    6. Assertions on `empty_exp`:
       - `os.path.isfile(os.path.join(tmpdir, "runs", "legacy", "cavitydetection", "empty_exp", "results.json.v1"))` is False (no backup created because no regeneration ran for this dir).
       - The original empty `results.json` is unchanged.
    7. Final `print("✓ test_regenerate_flag passed")`.

    Add an additional test `test_regenerate_no_legacy_dir()`: in a fresh `tempfile.TemporaryDirectory()` with NO `runs/legacy/cavitydetection/` subtree, call `regenerate_results(tmpdir)` and assert it returns without raising. Print friendly success line.

    Wire both new tests into the `if __name__ == "__main__":` runner block (lines 354-401), following the existing pattern of `print("Running test_X..."); test_X(); print("  PASSED")`.
  </action>
  <verify>
    <automated>cd /home/jolilius/home/src/cavitydetection &amp;&amp; ../openevolve/.venv/bin/python -c "import ast; tree = ast.parse(open('openevolve/test_run_structure.py').read()); names = [n.name for n in tree.body if isinstance(n, ast.FunctionDef)]; assert 'test_regenerate_flag' in names, 'missing test_regenerate_flag'; assert 'test_regenerate_no_legacy_dir' in names, 'missing test_regenerate_no_legacy_dir'; print('OK: 2 new test fns defined')"</automated>
    <automated>cd /home/jolilius/home/src/cavitydetection &amp;&amp; ! ../openevolve/.venv/bin/python openevolve/test_run_structure.py 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `openevolve/test_run_structure.py` defines top-level `test_regenerate_flag` and `test_regenerate_no_legacy_dir` (AST check passes).
    - The test fixture creates the migrated tree `<tmpdir>/runs/legacy/cavitydetection/baseline/checkpoints/checkpoint_5/` and `.../checkpoint_10/` with both `best_program_info.json` and `best_program.c` files in each checkpoint dir.
    - The test asserts on byte-for-byte backup fidelity (the `results.json.v1` matches the original payload).
    - The test asserts the regenerated `results.json` has `iterations` of length 2 sorted by `checkpoint_iteration`.
    - The runner block at lines 354+ now includes `test_regenerate_flag` and `test_regenerate_no_legacy_dir` calls.
    - Running `../openevolve/.venv/bin/python openevolve/test_run_structure.py` exits NON-ZERO right now (Task 2 hasn't added `regenerate_results` yet — RED state). The negated-shell verify line above asserts this.
  </acceptance_criteria>
  <done>Two new test functions exist, exercise the full backup-then-regenerate flow, fail with `ImportError` or `AttributeError` until Task 2 lands `regenerate_results`.</done>
</task>

<task type="auto">
  <name>Task 2: Add regenerate_results() helper and --regenerate CLI flag to migrate_legacy.py</name>
  <files>openevolve/migrate_legacy.py</files>
  <read_first>
    - openevolve/migrate_legacy.py (full file — current `main()` lines 52-68 and `migrate_legacy()` lines 21-49)
    - .planning/phases/04-per-step-data-pipeline/04-CONTEXT.md (decisions D-09, D-10)
    - .planning/phases/04-per-step-data-pipeline/04-RESEARCH.md (Pattern 3 + Pitfall 5)
    - .planning/phases/04-per-step-data-pipeline/04-PATTERNS.md (`openevolve/migrate_legacy.py — add --regenerate flag` section)
  </read_first>
  <action>
    Edit `openevolve/migrate_legacy.py` to add `regenerate_results()` and wire it into `main()` via a new `--regenerate` flag.

    1. New top-level function `regenerate_results(output_root: str) -> None` placed after `migrate_legacy()` (between lines 49 and 52):
       - Import `consolidate_experiment` inline at the top of the function body with the try/except module-vs-script guard used elsewhere in the codebase:
         ```
         try:
             from consolidate_results import consolidate_experiment
         except ImportError:
             from .consolidate_results import consolidate_experiment
         ```
       - Compute `legacy_dir = os.path.join(output_root, "runs", "legacy", "cavitydetection")`.
       - If `not os.path.isdir(legacy_dir)`: print `f"No legacy directory found at {legacy_dir}, nothing to regenerate."` and return.
       - Iterate `sorted(os.listdir(legacy_dir))`. For each `name`:
         - `exp_dir = os.path.join(legacy_dir, name)`.
         - If not a directory, `continue`.
         - Pitfall 5 guard: if `not os.path.isdir(os.path.join(exp_dir, "checkpoints"))`, print `f"Skip (no checkpoints/): {name}"` and continue.
         - `results_path = os.path.join(exp_dir, "results.json")`; `backup_path = os.path.join(exp_dir, "results.json.v1")`.
         - If `os.path.isfile(results_path)`: call `shutil.copy2(results_path, backup_path)` BEFORE regeneration (D-10 — always backup first) and print `f"Backed up: runs/legacy/cavitydetection/{name}/results.json -> results.json.v1"`.
         - Wrap the regeneration in `try/except Exception as e:` — call `consolidate_experiment(output_dir=exp_dir)` (passing only `output_dir`; let the other args take their defaults — `prompt_variant` falls back to `os.path.basename(exp_dir.rstrip("/"))` which equals `name`). On success, print `f"Regenerated: runs/legacy/cavitydetection/{name}/results.json"`. On exception, print `f"Warning: regeneration failed for {name}: {e}"` to stderr and continue to the next experiment (do not re-raise — partial success is acceptable per D-10's rollback story).

    2. In `main()`:
       - Add a new argparse argument after `--output-root` (after line 62):
         - `parser.add_argument("--regenerate", action="store_true", default=False, help="Regenerate results.json from checkpoints for migrated experiments; backs up existing results.json as results.json.v1")`.
       - After the existing `migrate_legacy(output_root)` call and `print("Migration complete.")` (line 67-68), add:
         - `if args.regenerate: print("Regenerating results.json from checkpoints..."); regenerate_results(output_root); print("Regeneration complete.")`

    3. The existing `migrate_legacy()` body, signature, and idempotency behavior MUST be untouched. The new flag is opt-in (D-09 — migration alone without `--regenerate` only moves directories, unchanged behavior).

    4. Imports: `shutil` is already imported at the top of the file (line 15). No new top-level imports needed; the `consolidate_experiment` import is intentionally inline inside `regenerate_results()` to avoid creating a circular-import-style coupling with `consolidate_results.py` when `migrate_legacy.py` is loaded as a module for other reasons (e.g., during testing).
  </action>
  <verify>
    <automated>cd /home/jolilius/home/src/cavitydetection &amp;&amp; ../openevolve/.venv/bin/python -c "import ast; src = open('openevolve/migrate_legacy.py').read(); tree = ast.parse(src); names = [n.name for n in tree.body if isinstance(n, ast.FunctionDef)]; assert 'regenerate_results' in names, 'missing regenerate_results function'; assert '--regenerate' in src, 'missing --regenerate argparse flag'; assert 'results.json.v1' in src, 'missing v1 backup filename'; assert 'shutil.copy2' in src, 'missing shutil.copy2 backup call'; assert 'runs' in src and 'legacy' in src and 'cavitydetection' in src, 'missing legacy_dir path'; print('OK')"</automated>
    <automated>cd /home/jolilius/home/src/cavitydetection &amp;&amp; ../openevolve/.venv/bin/python openevolve/test_run_structure.py</automated>
    <automated>cd /home/jolilius/home/src/cavitydetection &amp;&amp; ../openevolve/.venv/bin/python openevolve/migrate_legacy.py --help | grep -E "(--regenerate|results.json.v1)"</automated>
  </verify>
  <acceptance_criteria>
    - `migrate_legacy.py` defines a top-level `regenerate_results` function (AST check).
    - Source string-contains `"--regenerate"`, `"results.json.v1"`, `"shutil.copy2"`, `"runs"`, `"legacy"`, `"cavitydetection"`.
    - Running `migrate_legacy.py --help` lists `--regenerate` and mentions the backup behavior.
    - `../openevolve/.venv/bin/python openevolve/test_run_structure.py` exits 0 and prints `"test_regenerate_flag" PASSED` and `"test_regenerate_no_legacy_dir" PASSED` lines.
    - Running `migrate_legacy.py` WITHOUT `--regenerate` on the existing `openevolve/openevolve_output/` directory still only moves directories (or skips them if already migrated) and does not create any `results.json.v1` files.
    - The function-internal import `from consolidate_results import consolidate_experiment` is present (verified by `grep -n 'from consolidate_results' openevolve/migrate_legacy.py` returning a line inside the `regenerate_results` body).
    - On a real post-migration tree (when one exists), running `migrate_legacy.py --regenerate` would:
      1. Print `Backed up:` for each experiment.
      2. Print `Regenerated:` for each experiment.
      3. Leave a `results.json.v1` file in each experiment directory.
      (Not verified by automated check because the live `runs/legacy/` tree does not yet exist on this branch per RESEARCH.md A1 / Open Questions; the synthetic test covers this behavior.)
  </acceptance_criteria>
  <done>`migrate_legacy.py` exposes a `--regenerate` flag and a `regenerate_results()` helper that backs up `results.json` to `results.json.v1` before calling `consolidate_experiment` (Plan 04-01) on each migrated experiment with a `checkpoints/` subdir. Tests pass; `--help` advertises the flag; default behavior (no flag) is unchanged.</done>
</task>

</tasks>

<verification>
- `../openevolve/.venv/bin/python openevolve/test_run_structure.py` passes (includes all 8 pre-existing tests plus the 2 new tests added in Task 1).
- `../openevolve/.venv/bin/python openevolve/test_consolidation.py` still passes (Plan 04-01's suite isn't regressed by Plan 04-02 or 04-03).
- `../openevolve/.venv/bin/python openevolve/migrate_legacy.py --help` advertises the new flag with a clear description.
- Manual / out-of-scope until Phase 3's migration is actually executed on real data: running `migrate_legacy.py --regenerate` against `openevolve/openevolve_output/` after `migrate_legacy.py` has actually been run to move `baseline/` and `prompt1/` into `runs/legacy/cavitydetection/`. Currently the legacy dirs are at the flat root; once migration is run, regeneration becomes meaningful end-to-end.
</verification>

<success_criteria>
- D-09 satisfied: `migrate_legacy.py` has `--regenerate` flag (opt-in); regeneration iterates migrated experiment dirs; backup created before overwrite.
- D-10 satisfied: `results.json.v1` is always created before `consolidate_experiment` writes the new `results.json`; verified byte-for-byte by `test_regenerate_flag`.
- Pitfall 5 avoided: the function scans `runs/legacy/cavitydetection/<name>/` only, and skips any subdir without a `checkpoints/` child. Verified by the `empty_exp` fixture path in `test_regenerate_flag` and by `test_regenerate_no_legacy_dir`.
- No regression in `migrate_legacy()` behavior: idempotency and skip-on-conflict logic preserved (existing `test_migration_moves` and `test_migration_idempotent` continue to pass).
</success_criteria>

<output>
After completion, create `.planning/phases/04-per-step-data-pipeline/04-03-SUMMARY.md` recording: the `--regenerate` CLI help output, which existing tests passed, and a note on whether `migrate_legacy.py --regenerate` has been run against the real `openevolve_output/` tree (probably not, since baseline/prompt1 are still at the flat root per the codebase snapshot — leave this as a manual follow-up for the user).
</output>
